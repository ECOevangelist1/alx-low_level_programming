#include <main.h>
#include <stdio.h>

/**
 * main - print the name of the program.
 * @argc: Number of command line arguments
 * @argv: Array name
 *
 * Return: always 0.
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
