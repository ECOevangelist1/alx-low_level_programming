#include <stdio.h>
/**
 * main - main function entry
 *
 * print letter of the alphabet
 * Return: int(0)
 */
int main(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; letter++)
	{
		putchar(letter);
	}
	for (letter = 'A'; letter <= 'Z'; letetr++)
	{
		putchar(letter);
	}
	putchar('\n');
	return (0);
}
